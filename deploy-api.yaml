apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-api
  namespace: code-runner-env
  labels:
    app: mcp-api
    component: api
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mcp-api
      component: api
  template:
    metadata:
      labels:
        app: mcp-api
        component: api
        type: gpu
    spec:
      serviceAccountName: restricted-app-sa
      automountServiceAccountToken: true
      nodeSelector:
        workload: "ai"
      imagePullSecrets:
        - name: ghsec
      containers:
        - name: app
          image: "pregmatch/shiny-flake:mcp-api-${IMAGE_TAG}"
          ports:
            - containerPort: 8000
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "compute,utility"
            - name: HF_HOME
              value: "/app/hub"
            - name: TRANSFORMERS_OFFLINE
              value: "1"
            - name: HF_HUB_OFFLINE
              value: "1"
            - name: include_in_schema
              value: "False"
            - name: OLLAMA_BASE_URL
              value: "http://ollama-service.default.svc.cluster.local:11434"
            - name: OLLAMA_MODEL
              value: "qwen2.5-coder:7b"
---
# 2. API SERVICE
apiVersion: v1
kind: Service
metadata:
  name: mcp-api-service
  namespace: code-runner-env
spec:
  selector:
    app: mcp-api
    component: api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
---
# 3. API INGRESS
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-api
  namespace: code-runner-env
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/buffering: "false"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - mcp.api.intovoid.dev
      secretName: mcp-api-intovoid-dev-tls
  rules:
    - host: mcp.api.intovoid.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-api-service
                port:
                  number: 8000